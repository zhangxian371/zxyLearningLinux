# linux-cpp-starter

Minimal Linux C++ project template — builds with CMake from the command line.

## Build
```bash
cmake -S . -B build
cmake --build build
```

## Run
```bash
./build/linux-cpp-starter
```

## Clean
```bash
rm -rf build
```
